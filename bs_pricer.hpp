#ifndef BS_PRICER_HPP#define BS_PRICER_HPP#include <string>struct PriceResult {    double price;    double d1;    double d2;};struct Greeks {    double delta;    double gamma;    double vega;    double theta;    double rho;};PriceResult bs_price(    double S,    double K,    double T,    double r,    double vol,    const std::string& option_type = "call");Greeks bs_greeks(    double S,    double K,    double T,    double r,    double vol,    const std::string& option_type = "call");double implied_vol(    double market_price,    double S,    double K,    double T,    double r,    const std::string& option_type = "call",    double initial_guess = 0.2,    double tolerance = 1e-6,    int max_iterations = 100);void check_put_call_parity(    double S,    double K,    double T,    double r,    double vol);#endif